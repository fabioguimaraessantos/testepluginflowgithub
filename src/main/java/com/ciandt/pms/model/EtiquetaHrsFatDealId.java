/*
 * @(#) EtiquetaHrsFatDealId.java
 * Copyright (c) 2008 Ci&T Software S/A.
 * All Rights Reserved.
 */
package com.ciandt.pms.model;

import javax.persistence.Column;
import javax.persistence.Embeddable;

/**
 * EtiquetaHrsFatDealId generated by hbm2java.
 *
 * @author Generated by Hibernate Tools 3.2.4.GA
 * @since 02/12/2009 10:30:07
 * @version 09/01/19 1.1 10:08:00
 */
@Embeddable
public class EtiquetaHrsFatDealId implements java.io.Serializable {

    /**
     * Valor do serialVersionUID.
     */
    private static final long serialVersionUID = 1L;

    /** 
     * Constante para NamedQuery "EtiquetaHrsFatDealId.findAll".
     */
    public static final String FIND_ALL = "EtiquetaHrsFatDealId.findAll";

    /**
     * Atributo gerado a partir da coluna ETIQ_CD_ETIQUETA.
     */
    @Column(name = "ETIQ_CD_ETIQUETA", nullable = false, precision = 18, scale = 0)
    private Long codigoEtiqueta;

    /**
     * Atributo gerado a partir da coluna IHFD_CD_ITEM_HRS_FAT_DEAL.
     */
    @Column(name = "IHFD_CD_ITEM_HRS_FAT_DEAL", nullable = false, precision = 18, scale = 0)
    private Long codigoItemHrsFatDeal;

    /**
     * Construtor default.
     */
    public EtiquetaHrsFatDealId() {
    }

    /**
     * Construtor com preenchimento da entidade.
     * @param codigoEtiqueta Valor do atributo codigoEtiqueta;
     * @param codigoItemHrsFatDeal Valor do atributo codigoItemHrsFatDeal;
     */
    public EtiquetaHrsFatDealId(final Long codigoEtiqueta, final Long codigoItemHrsFatDeal) {
        this.codigoEtiqueta = codigoEtiqueta;
        this.codigoItemHrsFatDeal = codigoItemHrsFatDeal;
    }

    /**
     * Obtem o valor do atributo codigoEtiqueta.<BR>

     * @return Valor do atributo codigoEtiqueta.
     */
    public Long getCodigoEtiqueta() {
        return this.codigoEtiqueta;
    }

    /**
     * Atualiza o valor do atributo codigoEtiqueta.<BR>

     * @param codigoEtiqueta Novo valor para o atributo codigoEtiqueta.
     */
    public void setCodigoEtiqueta(final Long codigoEtiqueta) {
        this.codigoEtiqueta = codigoEtiqueta;
    }

    /**
     * Obtem o valor do atributo codigoItemHrsFatDeal.<BR>

     * @return Valor do atributo codigoItemHrsFatDeal.
     */
    public Long getCodigoItemHrsFatDeal() {
        return this.codigoItemHrsFatDeal;
    }

    /**
     * Atualiza o valor do atributo codigoItemHrsFatDeal.<BR>

     * @param codigoItemHrsFatDeal Novo valor para o atributo codigoItemHrsFatDeal.
     */
    public void setCodigoItemHrsFatDeal(final Long codigoItemHrsFatDeal) {
        this.codigoItemHrsFatDeal = codigoItemHrsFatDeal;
    }

    /**
     * @see Object#equals(Object)
     * 
     * @param other
     *            Valor do atributo other;
     * @return true se os objetos sao iguais, caso contrário retorna false
     */
    @Override
    public boolean equals(final Object other) {
        if ((this == other)) {
            return true;
        }
        if ((other == null)) {
            return false;
        } 
        if (!(other instanceof EtiquetaHrsFatDealId)) {
            return false;
        }
        EtiquetaHrsFatDealId castOther = (EtiquetaHrsFatDealId) other;

        return ((this.getCodigoEtiqueta() == castOther.getCodigoEtiqueta()) || (this
                .getCodigoEtiqueta() != null
                && castOther.getCodigoEtiqueta() != null && this
                .getCodigoEtiqueta().equals(castOther.getCodigoEtiqueta())))
                && ((this.getCodigoItemHrsFatDeal() == castOther
                        .getCodigoItemHrsFatDeal()) || (this
                        .getCodigoItemHrsFatDeal() != null
                        && castOther.getCodigoItemHrsFatDeal() != null && this
                        .getCodigoItemHrsFatDeal().equals(
                                castOther.getCodigoItemHrsFatDeal())));
    }

    /**
     * @see Object#hashCode()
     * @return HashCode do Objeto
     */
    @Override
    public int hashCode() {
        int result = 17;

        result = 37
                * result
                + (getCodigoEtiqueta() == null ? 0 : this.getCodigoEtiqueta()
                        .hashCode());
        result = 37
                * result
                + (getCodigoItemHrsFatDeal() == null ? 0 : this
                        .getCodigoItemHrsFatDeal().hashCode());
        return result;
    }

}